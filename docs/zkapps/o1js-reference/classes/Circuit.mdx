## Constructors

### new Circuit()

```ts
new Circuit(): Circuit
```

#### Returns

[`Circuit`](Circuit.mdx)

## Properties

### \_main

```ts
static _main: CircuitData<any, any>;
```

#### Source

[lib/proof-system/circuit.ts:22](https://github.com/o1-labs/o1js/blob/000b9835d52eede806be269b958c57000ff32f72/src/lib/proof-system/circuit.ts#L22)

## Methods

### generateKeypair()

```ts
static generateKeypair(): Promise<Keypair>
```

Generates a proving key and a verification key for this circuit.

#### Returns

`Promise`\<[`Keypair`](Keypair.mdx)\>

#### Example

```ts
const keypair = await MyCircuit.generateKeypair();
```

#### Source

[lib/proof-system/circuit.ts:31](https://github.com/o1-labs/o1js/blob/000b9835d52eede806be269b958c57000ff32f72/src/lib/proof-system/circuit.ts#L31)

***

### prove()

```ts
static prove(
   privateInput, 
   publicInput, 
keypair): Promise<Proof>
```

Proves a statement using the private input, public input, and the [Keypair](Keypair.mdx) of the circuit.

#### Parameters

• **privateInput**: `any`[]

• **publicInput**: `any`[]

• **keypair**: [`Keypair`](Keypair.mdx)

#### Returns

`Promise`\<`Proof`\>

#### Example

```ts
const keypair = await MyCircuit.generateKeypair();
const proof = await MyCircuit.prove(privateInput, publicInput, keypair);
```

#### Source

[lib/proof-system/circuit.ts:50](https://github.com/o1-labs/o1js/blob/000b9835d52eede806be269b958c57000ff32f72/src/lib/proof-system/circuit.ts#L50)

***

### verify()

```ts
static verify(
   publicInput, 
   verificationKey, 
proof): Promise<boolean>
```

Verifies a proof using the public input, the proof, and the initial [Keypair](Keypair.mdx) of the circuit.

#### Parameters

• **publicInput**: `any`[]

• **verificationKey**: `VerificationKey`

• **proof**: `Proof`

#### Returns

`Promise`\<`boolean`\>

#### Example

```ts
const keypair = await MyCircuit.generateKeypair();
const proof = await MyCircuit.prove(privateInput, publicInput, keypair);
const isValid = await MyCircuit.verify(publicInput, keypair.vk, proof);
```

#### Source

[lib/proof-system/circuit.ts:76](https://github.com/o1-labs/o1js/blob/000b9835d52eede806be269b958c57000ff32f72/src/lib/proof-system/circuit.ts#L76)
